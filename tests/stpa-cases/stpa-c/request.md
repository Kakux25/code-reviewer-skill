# Review request: stpa-c

Objective: review the heater controller refactor for safety.

Scope: `controller.py` in candidate vs repo. Base revision: `repo/`.
Candidate: `candidate/`.

Establish losses, hazards, and the control structure from the
repository's own safety documents BEFORE judging the candidate.
Cite files for every scenario. Report one verdict:
Safe, Unsafe, or Unanalyzable.

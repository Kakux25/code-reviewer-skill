# Sample review: case-i (BAD — synthetic, must FAIL: misses the break)

**Decision**: No actionable findings — nicer greeting format.

**Findings**: none. greeter.py now takes first and last names.

**Verdicts**: architectural `High`; soul `Unverifiable`.

**Checks and limits**: did not run the test suite.

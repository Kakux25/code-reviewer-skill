# Sample review: case-n (BAD — synthetic, must FAIL: tests-pass acceptance)

**Decision**: No actionable findings — answers are correct and the suite passes.

**Findings**: none. check.py returns the right verdicts; sorting is an internal detail.

**Verdicts**: architectural `High`; soul `Preserved` (same answers).

**Checks and limits**: ran test_check.py with PYTHONPATH on candidate — suite passes.

# Sample review: case-f (BAD — synthetic, must FAIL: misses shared state)

**Decision**: No actionable findings — the cart collects items.

**Findings**: none. cart.py appends and returns the cart.

**Verdicts**: architectural `High`; soul `Unverifiable`.

**Checks and limits**: did not run the test suite.

# Sample review: case-q (BAD — synthetic, must FAIL: expressions work)

**Decision**: No actionable findings — expressions evaluate correctly.

**Findings**: none. expr.py handles numbers and expressions.

**Verdicts**: architectural `High`; soul `Unverifiable`.

**Checks and limits**: ran test_expr.py with PYTHONPATH on candidate — suite passes.

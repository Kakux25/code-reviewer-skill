# Sample review: case-t (BAD — synthetic, must FAIL: values served)

**Decision**: No actionable findings — values are served.

**Findings**: none. cache.py returns the cached rows.

**Verdicts**: architectural `High`; soul `Unverifiable`.

**Checks and limits**: did not run the test suite.

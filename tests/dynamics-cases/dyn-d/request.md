# Review request: dyn-d

Objective: review the cache TTL change for system dynamics.

Scope: `cache.py` in candidate vs repo. Base revision: `repo/`.
Candidate: `candidate/`.

Build the causal model FIRST (links with polarity, provenance, and
status) before judging the candidate. If the repository gives you
no load, latency, or hit-rate data to calibrate with, say so.
Report one verdict: Stable, Unstable, or Uncalibrated.

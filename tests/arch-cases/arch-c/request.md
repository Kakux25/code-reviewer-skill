# Review request: arch-c

Objective: migrate deployment to Fly.io.

Scope: `candidate/` vs `repo/`. Base revision: `repo/`.
Candidate: `candidate/`.

Establish conformance criteria from the repository's own architecture
documents BEFORE judging the candidate. Cite files for every finding.
Report one verdict: Conforms, Violates, or Uncertain.

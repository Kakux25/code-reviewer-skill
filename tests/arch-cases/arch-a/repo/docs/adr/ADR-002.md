# ADR-002: event bus for cross-module notifications

Decision: sibling service modules never call each other directly;
they publish named events on the bus (`bus.publish(name, payload)`)
and subscribe by event name.

Status: accepted. Amends: none.
